# DST Submittals Generator V2 - Requirements (Mac/Linux)
# Minimal dependencies for Gotenberg-based conversion

# Core Dependencies
pypdf>=3.0.0
reportlab>=4.0.0
Pillow>=10.0.0
requests>=2.31.0

# Web Interface Dependencies
Flask>=2.3.0
Werkzeug>=2.3.0

# Keep python-docx for now (some legacy code still needs it)
python-docx>=0.8.11
